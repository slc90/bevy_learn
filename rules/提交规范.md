## Git 提交规范

- Git message 必须使用中文。

- Git message 必须分成标题和内容：标题要简洁；标题和内容之间固定空一行；内容必须使用 `1.`、`2.` 这样的数字序号逐条写清楚，各条之间不要空行。

- 提交前必须运行所有单元测试，并使用 `tokei` 统计 Rust 代码注释行数；如果单元测试不通过，或者中文注释占比不满足规范要求，禁止提交，继续修改。

- 禁止自己推送到远端仓库。
